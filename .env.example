# Backend
PORT=8080
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
URL_FETCH_TIMEOUT_MS=10000
# Optional server default OpenAI key. If omitted, clients can provide their own via the `X-OpenAI-Key` header.
OPENAI_API_KEY=

# Frontend
VITE_API_BASE=/api
